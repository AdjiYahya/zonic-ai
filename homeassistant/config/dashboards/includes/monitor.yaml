type: vertical-stack

cards:

  - type: markdown
    content: |
      ## 🖥️ System

  - type: grid
    columns: 2
    square: false

    cards:

      - type: gauge
        entity: sensor.system_monitor_memory_use
        name: Memory
        max: 4000

      - type: gauge
        entity: sensor.system_monitor_disk_use
        name: Storage
        max: 100